    
    /**
     * Clase Moneda: Encargada de la generación de monedas cuando se completa una lección, teniendo en cuenta
     * cuenta el numero de aciertos y errores, realizará el cálculo respectivo y transferirá las monedas al usuario. 
     * 
     * @author (CardenasJeisson & VargasLaura) 
     * @version (1.0.0)
     */
    public class Moneda
    {
        private Leccion leccion;
        private Usuario usuario;
    
        /**
         * @param leccion: Para saber para cual lección completó el usuario y
         * así poder realizar el cálculo de monedas.
         * @param usuario: Para saber cual usuario fue el que completó la lección.
         */
        
        public void calculoMonedas(){
            /*
            * Genera el cálculo de monedas ganadas por el usuario en una lección
            */
        } 
        public void entregaMonedas(Usuario usuario){
            /*
            * Hace entrega de las monedas al usuario a partir del cálculo anterior
            */
        }
}
